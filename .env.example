VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Keep AI provider keys and Supabase service-role keys on the server only.
# Do not prefix private secrets with VITE_, because Vite exposes them to browser code.
